"use client";

import TourSlider from "@/components/TourSlider/TourSlider";

export default function Tours() {

  return (
    <TourSlider
      headingId="tours-heading"
      heading={"Four Days We Know by Heart — and One We Don't Yet"}
      lede={"Four are routes we run most, refined over hundreds of days on the water: when to leave so the cave is lit, which bay empties out by two, where lunch is worth the detour. The fifth is blank — your day, built from scratch."}
      pullUp
    />
  );
}
